# Production env vars for Vercel build
# Replace the placeholder below with your actual Render service URL
# then set this same value in: Vercel Dashboard → Project → Settings → Environment Variables
# GCP Cloud Run (see docs/GCP_API_DEPLOY.md) — set in Vercel Production env
VITE_API_BASE_URL=https://tradetalk-api-933081724691.us-central1.run.app

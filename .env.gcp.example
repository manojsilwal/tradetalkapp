# Copy to .env.gcp on the GCP VM (gitignored). Do not commit secrets.
# Non-secret host/db/user defaults are also in backend/gcp_settings.py and docker-compose.gcp.yml.

PORTFOLIO_STORAGE=postgres
POSTGRES_HOST=34.31.98.184
POSTGRES_PORT=5432
POSTGRES_DB=tradetalk
POSTGRES_USER=tradetalk
POSTGRES_PASSWORD=

# Existing production secrets (OpenRouter, Supabase, JWT, etc.) stay in this file too.

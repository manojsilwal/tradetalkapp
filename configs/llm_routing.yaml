# Predictor synthesis / reviewer routing — OpenRouter primary, Gemini 3.5 Flash fallback.
# Temperature and max_tokens are read here; model slugs come from OPENROUTER_MODEL / GEMINI_MODEL.

predictor:
  synthesis:
    temperature: 0.2
    max_tokens: 1500
  reviewer:
    temperature: 0.0
    max_tokens: 800

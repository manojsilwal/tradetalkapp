{
  "servers": {
    "tradetalk": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "tradetalk_mcp"],
      "env": {
        "TRADETALK_ROOT": "${workspaceFolder}",
        "TRADETALK_MCP_MODE": "context"
      }
    }
  }
}

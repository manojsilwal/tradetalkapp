# TradeTalk FastAPI backend is deployed on GCP Cloud Run only.
# See docs/GCP_API_DEPLOY.md and scripts/deploy_api_cloudrun.sh
#
# FinCrawler is an optional separate service (may still be hosted on Render).
# Do not add tradetalkapp-backend here.

services: []
